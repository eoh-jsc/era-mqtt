[uwsgi]
chdir = /app
module = app:app

master = true
enable-threads = true
single-interpreter = true
processes = 2
threads = 1

socket = :5000
protocol = http
chmod-socket = 660

vacuum = true
die-on-term = true

logto = /var/log/uwsgi.log

for-readline = /app/.env
  env = %(_)
endfor =
