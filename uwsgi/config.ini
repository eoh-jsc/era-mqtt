[uwsgi]
chdir = /app
module = app:app

master = true
enable-threads = true
single-interpreter = true
processes = 2
threads = 1

socket = /tmp/uwsgi.sock
chmod-socket = 777

vacuum = true
die-on-term = true

logto = /var/log/uwsgi/uwsgi.log

for-readline = /app/.env
  env = %(_)
endfor =
